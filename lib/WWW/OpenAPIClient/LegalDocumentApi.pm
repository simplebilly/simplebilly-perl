=begin comment

SimpleBilly API

Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.

The version of the OpenAPI document: 0.1.0
Contact: support@simplebilly.com
Generated by: https://openapi-generator.tech

=end comment

=cut

#
# NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
# Do not edit the class manually.
# Ref: https://openapi-generator.tech
#
package WWW::OpenAPIClient::LegalDocumentApi;

require 5.6.0;
use strict;
use warnings;
use utf8;
use Exporter;
use Carp qw( croak );
use Log::Any qw($log);

use WWW::OpenAPIClient::ApiClient;

use base "Class::Data::Inheritable";

__PACKAGE__->mk_classdata('method_documentation' => {});

sub new {
    my $class = shift;
    my $api_client;

    if ($_[0] && ref $_[0] && ref $_[0] eq 'WWW::OpenAPIClient::ApiClient' ) {
        $api_client = $_[0];
    } else {
        $api_client = WWW::OpenAPIClient::ApiClient->new(@_);
    }

    bless { api_client => $api_client }, $class;

}


#
# get_legal_documents
#
# List all legal documents of the tenant. Missing documents are seeded from the default texts (with tenant placeholders replaced) on first access.
#
{
    my $params = {
    };
    __PACKAGE__->method_documentation->{ 'get_legal_documents' } = {
        summary => 'List all legal documents of the tenant. Missing documents are seeded from the default texts (with tenant placeholders replaced) on first access.',
        params => $params,
        returns => 'ARRAY[LegalDocument]',
        };
}
# @return ARRAY[LegalDocument]
#
sub get_legal_documents {
    my ($self, %args) = @_;

    # parse inputs
    my $_resource_path = '/api/v1/legal/documents';

    my $_method = 'GET';
    my $query_params = {};
    my $header_params = {};
    my $form_params = {};

    # 'Accept' and 'Content-Type' header
    my $_header_accept = $self->{api_client}->select_header_accept('application/json');
    if ($_header_accept) {
        $header_params->{'Accept'} = $_header_accept;
    }
    $header_params->{'Content-Type'} = $self->{api_client}->select_header_content_type();

    my $_body_data;
    # authentication setting, if any
    my $auth_settings = [qw(bearer_token )];

    # make the API Call
    my $response = $self->{api_client}->call_api($_resource_path, $_method,
                                           $query_params, $form_params,
                                           $header_params, $_body_data, $auth_settings);
    if (!$response) {
        return;
    }
    my $_response_object = $self->{api_client}->deserialize('ARRAY[LegalDocument]', $response);
    return $_response_object;
}

#
# reset_legal_documents
#
# Restore default texts for all documents (or a single doc_type/lang when the optional filter is given). Returns the full tenant list.
#
# @param LegalDocumentReset $legal_document_reset  (required)
{
    my $params = {
    'legal_document_reset' => {
        data_type => 'LegalDocumentReset',
        description => '',
        required => '1',
    },
    };
    __PACKAGE__->method_documentation->{ 'reset_legal_documents' } = {
        summary => 'Restore default texts for all documents (or a single doc_type/lang when the optional filter is given). Returns the full tenant list.',
        params => $params,
        returns => 'ARRAY[LegalDocument]',
        };
}
# @return ARRAY[LegalDocument]
#
sub reset_legal_documents {
    my ($self, %args) = @_;

    # verify the required parameter 'legal_document_reset' is set
    unless (exists $args{'legal_document_reset'}) {
      croak("Missing the required parameter 'legal_document_reset' when calling reset_legal_documents");
    }

    # parse inputs
    my $_resource_path = '/api/v1/legal/documents/reset';

    my $_method = 'POST';
    my $query_params = {};
    my $header_params = {};
    my $form_params = {};

    # 'Accept' and 'Content-Type' header
    my $_header_accept = $self->{api_client}->select_header_accept('application/json');
    if ($_header_accept) {
        $header_params->{'Accept'} = $_header_accept;
    }
    $header_params->{'Content-Type'} = $self->{api_client}->select_header_content_type('application/json');

    my $_body_data;
    # body params
    if ( exists $args{'legal_document_reset'}) {
        $_body_data = $args{'legal_document_reset'};
    }

    # authentication setting, if any
    my $auth_settings = [qw(bearer_token )];

    # make the API Call
    my $response = $self->{api_client}->call_api($_resource_path, $_method,
                                           $query_params, $form_params,
                                           $header_params, $_body_data, $auth_settings);
    if (!$response) {
        return;
    }
    my $_response_object = $self->{api_client}->deserialize('ARRAY[LegalDocument]', $response);
    return $_response_object;
}

#
# upsert_legal_documents
#
# Upsert legal documents per (doc_type, lang). Returns the full tenant list.
#
# @param ARRAY[LegalDocumentUpsert] $legal_document_upsert  (required)
{
    my $params = {
    'legal_document_upsert' => {
        data_type => 'ARRAY[LegalDocumentUpsert]',
        description => '',
        required => '1',
    },
    };
    __PACKAGE__->method_documentation->{ 'upsert_legal_documents' } = {
        summary => 'Upsert legal documents per (doc_type, lang). Returns the full tenant list.',
        params => $params,
        returns => 'ARRAY[LegalDocument]',
        };
}
# @return ARRAY[LegalDocument]
#
sub upsert_legal_documents {
    my ($self, %args) = @_;

    # verify the required parameter 'legal_document_upsert' is set
    unless (exists $args{'legal_document_upsert'}) {
      croak("Missing the required parameter 'legal_document_upsert' when calling upsert_legal_documents");
    }

    # parse inputs
    my $_resource_path = '/api/v1/legal/documents';

    my $_method = 'PUT';
    my $query_params = {};
    my $header_params = {};
    my $form_params = {};

    # 'Accept' and 'Content-Type' header
    my $_header_accept = $self->{api_client}->select_header_accept('application/json');
    if ($_header_accept) {
        $header_params->{'Accept'} = $_header_accept;
    }
    $header_params->{'Content-Type'} = $self->{api_client}->select_header_content_type('application/json');

    my $_body_data;
    # body params
    if ( exists $args{'legal_document_upsert'}) {
        $_body_data = $args{'legal_document_upsert'};
    }

    # authentication setting, if any
    my $auth_settings = [qw(bearer_token )];

    # make the API Call
    my $response = $self->{api_client}->call_api($_resource_path, $_method,
                                           $query_params, $form_params,
                                           $header_params, $_body_data, $auth_settings);
    if (!$response) {
        return;
    }
    my $_response_object = $self->{api_client}->deserialize('ARRAY[LegalDocument]', $response);
    return $_response_object;
}

1;
